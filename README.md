# סדנת AI מונגשת לחירשים וכבדי שמיעה

Single-page landing site (HTML/CSS/JS, no build step).

- `index.html` — the entire site
- `public/` — instructor photos and showcase assets

Run locally: open `index.html` in a browser.
